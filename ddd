elif opcion == "3":
        id_buscado = int(input("ingresar id a buscar : "))
        encontrado = buscar_estudiante(inventario,id_buscado)
        if encontrado:
            print("estudiante existente", encontrado)
        else:
            print("el estudiante no extiste en el inventario")
def buscar_estudiante(inventario,id_buscado):
    for gestion_estuden in inventario:
        if gestion_estuden["id_estudiante"] == id_buscado:
            return gestion_estuden
    return None
